import 'package:flutter/material.dart';

class LocalAuthMobile extends StatelessWidget {
  const LocalAuthMobile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
